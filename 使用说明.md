# 调试工具箱（类似“图吧工具箱”的快捷启动器）

把常用软件集中成一个个彩色按钮，点击即可打开：

- **本地程序**（.exe 等）—— 一键启动，可勾选“以管理员身份运行”
- **网址 / 路由器后台** —— 用默认浏览器打开，**直接填 IP 也行**（如 `192.168.1.1`，自动补全为 http://）
- **文件夹** —— 直接在资源管理器中打开

内置两个实用工具：
- **IP 扫描**：类似 Angry IP Scanner，扫网段内在线设备、找路由器/Web管理页
- **16进制报文翻译器**：粘贴 101/104 报文或国网安全报文，逐字段翻译并指出问题

按钮的**名称、路径、分类、颜色、图标**全部可以自定义。

---

## 一、怎么启动

### 方式 1：exe 版（推荐，无需安装任何东西）
直接双击 **`调试工具箱.exe`** 即可使用，不需要电脑装有 Python。
（提示：个别杀毒软件可能误报单文件 exe，加入信任即可，这是打包工具的常见现象。）

### 方式 2：Python 版
双击 **`启动调试工具箱.bat`** 即可（不弹黑色命令行窗口），
或双击 `调试工具箱.py`，或在命令行运行 `python 调试工具箱.py`。

首次打开会看到几个示例按钮（记事本、计算器、路由器后台），
用它们试一下效果，然后按下面的方法改成你自己的即可。

## 二、添加你自己的软件

### 方法 1：自动扫描（推荐，最省事）
如果你的 FTU 软件放在一个文件夹里，每个子文件夹是一个厂家，例如：

```
D:\FTU工具\
  ├─ 厂家A\  FTU调试.exe  附属工具.exe
  ├─ 厂家B\  config.exe
  └─ 厂家C\  ...
```

1. 点右上角 **「扫描文件夹」**
2. 选择 `D:\FTU工具`
3. 勾选 **“包含子文件夹”** 和 **“用子文件夹名作为分类”**（默认已勾选）
4. 在结果里选中要添加的（可全选），点 **「添加选中」**

这样会自动生成按钮，**厂家名自动变成分类**（厂家A、厂家B…），
每个程序自动命名。之后右键按钮 → **编辑**，改成好记的名字即可。

### 方法 2：手动添加
点右上角 **「＋ 添加」**，填写：

- **按钮名称**：你想起的名字，例如“XX厂家 FTU调试”
- **类型**：
  - `程序 / 文件`：选本地 .exe，点“浏览…”选择即可
  - `网址 / 路由器 (IP)`：直接填 `192.168.1.1` 或 `http://192.168.1.1:8080`
  - `文件夹`：填或浏览一个文件夹
- **路径 / 网址**：本地文件路径，或 IP / 网址
- **分类**：选已有分类，或直接输入一个新分类名（会自动创建）
- **颜色 / 图标 / 备注**：可选
- **以管理员身份运行**：部分 FTU 软件需要管理员权限时勾选

填完可先点 **「测试打开」** 试试，再点 **「保存」**。

## 三、IP 扫描（内置，类似 Angry IP Scanner）

点右上角 **「IP扫描」** 打开：

1. 输入 IP 范围，支持多种写法：
   - `192.168.1.1-254`（从 .1 扫到 .254）
   - `192.168.1.0/24`
   - `192.168.1.*`
   - 单个 IP：`192.168.1.1`
2. 点 **「开始扫描」**，会自动并发 Ping 探测并列出在线设备（显示响应时间、主机名）
3. 勾选 **“检测常用端口”** 会顺便检查每个在线设备的 80/443/8080/22/23/3389 等端口，
   **能快速找到路由器/设备的 Web 管理页面**
4. **右键**结果：
   - 复制 IP
   - 用浏览器打开（http://IP）
   - **添加为工具箱按钮（网页）**——直接生成一个“Web管理 IP”按钮放进工具箱

## 四、16进制报文翻译器（平衡101规约 + 国网安全报文）

点右上角 **「报文翻译」** 打开：

1. 把抓到的报文粘贴进去（可一次贴多帧，支持空格/逗号/0x/连写）
2. 点 **「解析」**，会自动：
   - 区分固定帧（10…16）和长帧（68 L L 68…16）
   - 逐字段翻译：控制域（方向/FCB/FCV/DFC/功能码）、链路地址、
     类型标识TI、传送原因COT、公共地址、信息对象（遥信/遥测/遥控/时钟同步/总召等）
   - 解码 7 字节时标（CP56Time2a）、短浮点遥测值、遥控选择/执行、测试图像等
   - **识别国网安全报文**（EB…D7 开头），翻译报文类型/应用类型/随机数/签名/结果，明文内嵌的 101/104 报文会继续翻译
3. 红色 = 问题项，例如：
   - **校验和错误**（会给出正确值）
   - **长度不符**（帧长度和 L 不一致）
   - **报文不完整**（少了时标/数据等）
   - **未知类型标识 / 未知传送原因**
   - **对方回复否定**（如“未知的类型标识”0x2c、“未知的公共地址”0x2e 等）
   - 国网报文的 **验签失败/解密失败/认证失败** 等错误码

点 **「载入示例」** 可立即看到一段示例（含总召、复位链路、SOE、测试命令、请求链路状态、国网安全报文）。

## 五、日常使用

- **左键单击** 按钮 = 打开
- **右键** 按钮 = 打开 / 编辑 / 删除 / 打开所在文件夹 / 复制路径
- 顶部 **搜索框**：按名称、路径、备注快速过滤
- 左侧 **分类栏**：按厂家/类型筛选；右键分类栏可新建、重命名、改颜色、删除分类
- 右上角 **「编辑模式」**：开启后单击按钮直接进入编辑，避免误打开

## 六、配置文件

所有按钮和分类保存在程序同目录的 **`config.json`** 里
（exe 版和 Python 版共用这个文件，配置存在 exe 旁边）。
备份、换电脑时把这个文件和 `调试工具箱.exe` 一起复制即可。
程序运行日志写在 `debug.log`（方便排查问题）。

## 六.5、GitHub 发布与更新提示

程序托管在 GitHub：<https://github.com/arockwell233/debug-toolbox>（公开仓库）。

- **每次启动** 程序会自动检查 GitHub 上是否有新版本（可在 设置 → 在线更新 中关闭）
- 发现新版本会弹窗提示，点「下载新版」会打开 GitHub 页面，下载新版 exe 覆盖当前程序即可
- 以后有新功能/修复都会发版到这个 GitHub 仓库

## 七、常见问题

- **点了没反应**：看按钮是不是“网页”类型且 IP 不对（IP 变了就去右键编辑改掉）；本地程序路径变了也会打不开，右键编辑重新选即可。
- **软件提示没有权限**：右键按钮 → 编辑 → 勾选“以管理员身份运行”。
- **杀毒软件报警**：单文件 exe 偶尔被误报，加入信任名单即可；不放心可以用 Python 版（方式 2）。
- **想自己重新打包 exe**：联网安装打包工具后执行：
  `pip install pyinstaller`
  `pyinstaller -F -w --icon icon.ico --name 调试工具箱 调试工具箱.py`
  生成的文件在 `dist\调试工具箱.exe`。

## 八、程序文件说明

| 文件 | 说明 |
| --- | --- |
| 调试工具箱.exe | 打包好的程序，双击即用（推荐） |
| 调试工具箱.py | Python 主程序（需要电脑装有 Python） |
| ip_scanner.py | IP扫描功能模块 |
| hex_translator.py | 16进制报文翻译功能模块 |
| 启动调试工具箱.bat | Python 版启动脚本 |
| config.json | 你的按钮和分类配置 |
| 使用说明.md | 本说明 |
